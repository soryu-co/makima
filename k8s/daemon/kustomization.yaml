apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: makima-daemon

commonLabels:
  app.kubernetes.io/part-of: makima

resources:
  - deployment.yaml
  - configmap.yaml
  - secret.yaml
  - hpa.yaml
